- hosts: all
  tasks:
    # Setup uWSGI
    - include: tasks/uwsgi.yaml
    # Setup nginx
    - include: tasks/nginx-config.yaml
    # Setup certbot
    - include: tasks/certbot.yaml
    # Configure nginx site
    - include: tasks/nginx-site-config.yaml
    # Configure Let's Encrypt
    - include: tasks/letsencrypt.yaml
    # Configure uWSGI Emperor mode
    - include: tasks/uwsgi-emperor.yaml
